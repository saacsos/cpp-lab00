#include <iostream>
using namespace std;

unsigned long factorial(unsigned long n);

int main() {
  unsigned long n = 0;
  cin >> n;
  cout << factorial(n) << endl;
}

unsigned long factorial(unsigned long n) {
  return 0;
}